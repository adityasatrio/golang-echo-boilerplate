package service

import (
	"github.com/stretchr/testify/assert"
	"testing"
)

func TestBookServiceImpl_your_function_action(t *testing.T) {

	if assert.NoError(t, nil) {
		dataValue := "true"
		assert.Equal(t, "true", dataValue)
	}
}
